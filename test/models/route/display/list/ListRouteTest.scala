package models.route.display.list

import models.route.{Route, RoutePoint}
import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class ListRouteTest extends AnyFlatSpec with Matchers {
//  "List Route" should "create a list route from a route" in {
//    val route = Route(RoutePoint(0,0,"from", "from", "from"), RoutePoint(1, 1, "to", "to", "to"), "toc", "single", "electrification", "speed", "srs", "type", 0)
//    val listRoute = ListRoute(route)
//
//    listRoute.from should be(ListRoutePoint("from", "from"))
//    listRoute.to should be(ListRoutePoint("to", "to"))
//    listRoute.srs should be("srs")
//  }
}
